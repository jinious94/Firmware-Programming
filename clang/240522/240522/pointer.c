#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

//포인터 변수: 주소값을 저장하는 변수
//포인터 변수 선언: 데이터 타입 * 변수명

int main_c(){
	/*int* ptri;
	double* ptrd;
	char* ptrc;
	
	printf("int형의 포인터 크기 %d \n", sizeof(ptri));
	printf("double형의 포인터 크기 %d \n", sizeof(ptrd));
	printf("char형의 포인터 크기 %d \n", sizeof(ptrc));*/
	




	/*int a = 13;
	int* ptr = &a;
	int** pptr = &ptr;

	printf("a의 값 : %d \n", a);
	printf("ptr이 가리키는 주소의 값 : %d \n", *ptr);
	printf("pptr이 가리키는 주소의 값이 가리키는 값 : %d \n\n", **pptr);

	printf("a의 주소 : %x \n", &a);
	printf("ptr의 값 : %x \n", ptr);
	printf("pptr이 가리키는 주소의 값 : %x \n\n", *pptr);

	printf("ptr의 주소 : %x \n", &ptr);
	printf("pptr의 값 : %x \n\n", pptr);*/

	




	/*int a;
	int b;
	int* ptr;

	ptr = &a;
	*ptr = 2;

	ptr = &b;
	*ptr = 3;

	printf("a의 값 :  %d \n", a);
	printf("a의 값 :  %d \n", b);*/




	/*int* ptr;
	int arr[3] = { 10, 20, 30 };
	ptr = arr;

	printf("ptr[0] 값 : %d \n", ptr[0]);
	printf("arr[0]의 값 : %d \n\n", arr[0]);

	printf("arr*의 값 : %d \n", *(arr + 1));
	printf("ptr*의 값 : %d \n", *(ptr + 1));*/




	/*int arr[5] = { 1, 2, 3, 4, 5 };
	int* parr = arr;
	
	for (int i = 0; i < 5; i++) {
		printf("parrr = %p \t", parr);
		printf("*parr = %d \n", *parr++);
	}*/


	

	return 0;
}